# Folder used to save the weights of a trained model
WEIGHTS_BASE_DIR = "weights/"
# The extension used for saving the models. Should always be '.h5'
WEIGHTS_EXTENSION = ".h5"
# Add a timestamp to the weight filename
NAME_WITH_TIME = False

# Validation split value. Used if no validation data is specified
VALIDATION_SPLIT = 0.1
